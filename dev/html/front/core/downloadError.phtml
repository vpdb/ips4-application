<ips:template parameters="$message, $url" />

<p>Whoops, there was an error from VPDB when trying to download the file:</p>
<quote>{$message}</quote>

<a href="{$url}" class="ipsButton ipsButton_large ipsButton_important">Back</a>